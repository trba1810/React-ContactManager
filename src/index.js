import React from "react";
import ReactDOM from "react-dom/client";
import ContactIndex from "./Compontents/ContactPages/ContactIndex";
import "./style.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <div>
    <ContactIndex />
  </div>
);
